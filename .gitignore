# Rust build output. Large and fully reproducible.
target/

# NOTE: site/Masse.zip is deliberately NOT ignored. `railway up` respects
# .gitignore, so ignoring it empties the Docker build context and the deploy
# fails on COPY Masse.zip. It is the artifact the page actually serves.

# Generated by tools/make-icons.mjs at bundle time.
src/icons/

.DS_Store
