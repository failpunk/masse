# Rust build output. Large and fully reproducible.
target/

# The download is distributed via GitHub Releases, not served by the site, so the
# Dockerfile no longer copies it and ignoring it is safe again.
site/Masse.zip

# Generated by tools/make-icons.mjs at bundle time.
src/icons/

.DS_Store
